import { SearchFuelTypePipe } from './search-fuel-type.pipe';

describe('SearchFuelTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFuelTypePipe();
    expect(pipe).toBeTruthy();
  });
});
